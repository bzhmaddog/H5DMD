/**
 * @deprecated Use `width`, `height`, and `position` in layer options instead.
 */
export interface LayerDimensions {
    width?: number,
    height?: number,
    top?: number,
    left?: number,
    hAlign?: string,
    vAlign?: string,
    hOffset?: number,
    vOffset?: number
}
